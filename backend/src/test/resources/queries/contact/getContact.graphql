query ($uuid: String!) {
    contact(uuid: $uuid) {
        uuid, name, type, comment,
        phone, mobile,
        email, web,
        street, city, country, zip,
        icon {
            uuid,
        },
        created,
        modified
    }
}